Test:
    check monitor
    Test all on CPU
    check resume
    check connection
    check generate.py
    check mat_val
    check compat
    check predictor
    Test all on GPU
Configs:
    Loss:
        optimizer: adam, amsgrad, ttur
        loss: hinge, wgan_gp(+ct),  wgan_theirs+ct
        scheduler: num_epochs + transition + train
    Structure:
        progress: num layers, initial size, depth_manager_offset, progression_scale
        channel by channel
        stats layer
        num features: min, max, base, min=max
        residual
        kernel size: 3, 5, 7, 9, 11
        inception
        self attention: layer, size, num_samples
        separable: spreading_factor
    Normalization x2:
        dropout: mode, strength
        phase shuffle
        pixel norm
        spectral norm
        batch norm
        layer norm
        weight norm
    SeqLen:
        ConnectionNet: d_layer, network_structure, eval_metric
        PredictionNet: network_structure, eval_metric
        StaticCompatibilityNet: eval_metric
        DynamicCompatibilityNet: eval_metric, network_structure, d_layer
