TODO:
    dataset: more length + negative samples for D + G must generate fake (without global_z)
    **************************************************
    our_baseline: num_params + lr + batch_size + scheduler
    their_baseline: read the paper again
    our_baseline2: high batch_size => higher lr + scheduler + num_repeats
    our_hinge+SN: make a config with working hinge loss :D
    -------------------------------------------------
    act_norm: pixel, batch, layer, none
    param_norm: spectral, weight, none
    kernel_size: 3, 5, 7, 9
    wgan: orig, theirs x ct/not + num_repeats
    num features: 2048, 512, same, ...
    shapes: initial size, seq len, depth_offset
    stats layer: temporal, num_channels
    dropout: mode, strength x G/D
    exp: self_attention, ch_by_ch, hing+SN, gang, score_net
    condition: dataset, data_reader, evaluation
